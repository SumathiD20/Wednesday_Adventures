name: OWASP Security Scan - Backend

on:
  push:
    branches:
      - main
      - develop
      - l00187927-WA-Jira18-CIPipeline
  pull_request:
    branches:
      - main
      - develop
      - l00187927-WA-Jira18-CIPipeline

jobs:
  owasp-security-scan:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout Repository
      uses: actions/checkout@v4

    - name: Change to Backend Directory
      run: cd project-backend

    - name: Verify package.json exists
      run: |
        if [ ! -f project-backend/package.json ]; then
          echo "❌ package.json not found in project-backend!"
          exit 1
        fi

    - name: Show package.json Dependencies
      run: cat project-backend/package.json

    - name: Install Dependencies
      run: |
        cd project-backend
        rm -rf node_modules package-lock.json
        npm install

    - name: Verify Express Installation
      run: ls -R project-backend/node_modules || echo "❌ node_modules directory not found!"

    - name: Start Application Server
      run: |
        cd project-backend
        nohup node index.js &
        sleep 40

    - name: Check if Server is Running
      run: |
        curl -I http://localhost:5000 || (echo "❌ Server did not start!" && exit 1)

    - name: Run OWASP Dependency-Check
      uses: dependency-check/Dependency-Check_Action@main
      with:
        project: "project-backend"
        path: "./project-backend"
        format: "ALL"
        out: "./dependency-check-report"

    - name: Upload Dependency-Check Report
      uses: actions/upload-artifact@v4
      with:
        name: dependency-check-report
        path: dependency-check-report

    - name: Run OWASP ZAP Full Scan
      uses: zaproxy/action-full-scan@v0.11.0
      with:
        target: "http://localhost:5000"
        cmd_options: "-a"
        docker_name: ghcr.io/zaproxy/zaproxy:stable
        fail_action: false
        allow_issue_writing: false

    # ✅ NEW: Verify ZAP Report Exists Before Uploading
    - name: Check if ZAP Report Exists
      run: |
        if [ ! -f zap.out ]; then
          echo "❌ ZAP scan output file (zap.out) not found!"
          exit 1
        fi

    - name: Upload OWASP ZAP Report
      uses: actions/upload-artifact@v4
      with:
        name: zap-scan-report  # ✅ Updated valid artifact name
        path: zap.out

    - name: Fail Build on Critical/High Vulnerabilities
      run: |
        if [ ! -s dependency-check-report/dependency-check-report.json ]; then
          echo "⚠️ No dependency-check report found. Skipping failure check."
          exit 0
        fi
        CRITICAL_COUNT=$(jq '[.dependencies[]?.vulnerabilities[]? | select(.severity=="Critical")] | length' dependency-check-report/dependency-check-report.json)
        HIGH_COUNT=$(jq '[.dependencies[]?.vulnerabilities[]? | select(.severity=="High")] | length' dependency-check-report/dependency-check-report.json)
        echo "🔍 Found vulnerabilities:"
        echo "   🔴 Critical: $CRITICAL_COUNT"
        echo "   🟠 High: $HIGH_COUNT"
        if [ "$CRITICAL_COUNT" -gt 0 ]; then
          echo "❌ Critical vulnerabilities found! Failing the build."
          exit 1
        fi
        if [ "$HIGH_COUNT" -gt 5 ]; then
          echo "❌ More than 5 High vulnerabilities found! Failing the build."
          exit 1
        fi
        echo "✅ No critical or excessive high vulnerabilities found. Proceeding..."
