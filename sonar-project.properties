# Required metadata
sonar.projectKey=DarshanGowdaATU_Wednesday_Adventures
sonar.organization=your-organization-key

# Optional but recommended
sonar.projectName=Wednesday_Adventures
sonar.projectVersion=1.0

# Source code location
sonar.sources=src

# Tests location (only if tests exist)
sonar.tests=tests  # Update this to the correct path or remove it if tests don't exist

# Encoding
sonar.sourceEncoding=UTF-8
